=== raw hook JSON output ===
{
  "hookSpecificOutput": {
    "hookEventName": "SessionStart",
    "additionalContext": "feature-flow plugin is installed. For a non-trivial feature or a bug fix, prefer its durable, resumable, evidence-verified workflow instead of an ad-hoc edit: invoke the `feature-flow` skill, or run `/feature-flow:ff \"<your request>\"` to start a gated run. It auto-picks the feature track (explore->clarify->design->plan->implement->review->verify) or the test-first bugfix track (diagnose->regression-test->fix->verify->review), keeps all state in `.feature-flow/<slug>/`, and verifies with really-executed tests. Recover an interrupted run with `/feature-flow:ff-status` and `/feature-flow:ff-resume`.\n\nActive feature-flow run(s) in this project (from on-disk manifests). You were mid-run before context compaction — continue the most recent run below from its current phase, and re-read its manifest and artifacts from these paths rather than relying on the compacted summary:\n- ctl — feature/full · phase: implement (in_progress) · autopilot: off · signed off: yes · task: Task 2 (round 2) · resume: /feature-flow:ff-resume ctl\n  artifacts: ledger=.feature-flow/ctl/tasks/ledger.md"
  }
}

=== additionalContext ===
feature-flow plugin is installed. For a non-trivial feature or a bug fix, prefer its durable, resumable, evidence-verified workflow instead of an ad-hoc edit: invoke the `feature-flow` skill, or run `/feature-flow:ff "<your request>"` to start a gated run. It auto-picks the feature track (explore->clarify->design->plan->implement->review->verify) or the test-first bugfix track (diagnose->regression-test->fix->verify->review), keeps all state in `.feature-flow/<slug>/`, and verifies with really-executed tests. Recover an interrupted run with `/feature-flow:ff-status` and `/feature-flow:ff-resume`.

Active feature-flow run(s) in this project (from on-disk manifests). You were mid-run before context compaction — continue the most recent run below from its current phase, and re-read its manifest and artifacts from these paths rather than relying on the compacted summary:
- ctl — feature/full · phase: implement (in_progress) · autopilot: off · signed off: yes · task: Task 2 (round 2) · resume: /feature-flow:ff-resume ctl
  artifacts: ledger=.feature-flow/ctl/tasks/ledger.md

FOUND: 'task: Task 2 (round 2)'
