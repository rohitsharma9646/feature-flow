# Explore: version-flag

**Created:** 2026-09-24
**Track:** feature

## Summary

`src/cli.sh` is a small bash entry point that dispatches on `$1`. A `VERSION` file already sits at
the repo root, unused by any script.
