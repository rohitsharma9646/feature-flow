# e2e-1-forward-test-single-architect.md

kind: e2e/browser (actually a headless CLI end-to-end run of the shipped plugin via `claude -p`;
recorded under this file's `e2e` kind label per the dispatch's naming, though the correct
taxonomy tag for the per-record table below is `cli-output`/`e2e` — each run is a real, paid,
headless Claude Code session)

## 0. Preflight — confirm the capture script's only diff from the tracked script

command: `diff "$CAP" /home/netzwelt/feature-flow/scripts/forward-test.sh`
where CAP = /tmp/claude-1000/-home-netzwelt-feature-flow/26aa1176-a742-4f36-a57a-478450aeb348/scratchpad/repo-mirror/scripts/forward-test-capture.sh

exit status: 1 (diff found a difference, as expected — diff exits 1 when files differ)

output:
```
146d145
<         find "$tmp" -name architect.md -exec cp {} "$arm_out/architect.md" \;
```

Confirmed: the ONLY difference between the untracked capture script and the tracked
`scripts/forward-test.sh` is the added line copying `architect.md` out of the sandbox into the
arm's output dir. No other logic differs.

command: `bash "$CAP" --help`
exit status: 2 (usage — `-h/--help` exits 2 per the script's own contract)
output:
```
Usage:
  scripts/forward-test.sh [behavior...] [--repeat N] [--max-budget-usd X]
                          [--timeout SECS] [--output-dir DIR] [--model M]
Exit: 0 every selected arm PASS · 1 any FAIL/ERROR · 2 usage/precondition (nothing ran).
```

## 1. The run

command:
```
FF_FORWARD_PLUGIN_DIR=/home/netzwelt/feature-flow bash "$CAP" \
  --repeat 2 --max-budget-usd 3.00 \
  --output-dir /home/netzwelt/feature-flow/.feature-flow/single-architect-design/evidence/forward-new \
  single-architect
```
Run one at a time (no parallel forward-test processes), via Bash run_in_background (auto-backgrounded
after exceeding the 120s foreground timeout) and waited for completion.

exit status: 0
date (UTC, from summary.md): 2026-09-25T05:41:51Z
plugin: `/home/netzwelt/feature-flow` @ unknown (+ uncommitted changes) — HEAD has uncommitted
worktree changes for this feature branch, as expected mid-run

## 2. Console log (verbatim)

```
  single-architect/fired run 1/2: PASS ($0.3770788)
  single-architect/fired run 2/2: PASS ($0.33431040000000006)
PASS single-architect/fired  ($0.7114)
  single-architect/control run 1/2: PASS ($0.2712201)
  single-architect/control run 2/2: PASS ($0.3289161)
PASS single-architect/control  ($0.6001)
forward-test: 2/2 PASS, 0 FAIL, 0 ERROR · total $1.3115 · /home/netzwelt/feature-flow/.feature-flow/single-architect-design/evidence/forward-new
```

## 3. summary.md (generated by the script)

```
# Forward-test run

**Date (UTC):** 2026-09-25T05:41:51Z
**Plugin:** `/home/netzwelt/feature-flow` @ unknown (+ uncommitted changes)
**Repeat:** 2 · **Budget/run:** $3.00 · **Timeout/run:** 900s

| Behavior | Arm | Result | Cost | Note |
|---|---|---|---|---|
| single-architect | fired | PASS | $0.7114 |  |
| single-architect | control | PASS | $0.6001 |  |

**Summary:** 2/2 PASS, 0 FAIL, 0 ERROR · total $1.3115
```

## 4. Per-run detail

### fired arm (rate-limit spec — must fork)

| run | result | assert.sh findings | total_cost_usd | duration_ms | duration_api_ms | num_turns |
|---|---|---|---|---|---|---|
| 1 | PASS | architect.md written; `Recommended:` line; 3 `Rejected:` line(s), all 3 carrying complexity/risk/test-effort scores; no 'one obvious approach'; no source written | 0.3770788 | 82426 | 82132 | 6 |
| 2 | PASS | architect.md written; `Recommended:` line; 2 `Rejected:` line(s), both carrying scores; no 'one obvious approach'; no source written | 0.33431040000000006 | 92021 | 91667 | 6 |

assert-1.log (fired run 1, verbatim):
```
  ok:   architect.md written (/tmp/ff-forward-57qz8E/.feature-flow/rate-limit/architect.md)
  ok:   Recommended: line
  ok:   3 Rejected: line(s)
  ok:   every Rejected: line carries the three scores
  ok:   no 'one obvious approach' in the fired arm
  ok:   no source written
```

assert-2.log (fired run 2, verbatim):
```
  ok:   architect.md written (/tmp/ff-forward-b9pfuS/.feature-flow/rate-limit/architect.md)
  ok:   Recommended: line
  ok:   2 Rejected: line(s)
  ok:   every Rejected: line carries the three scores
  ok:   no 'one obvious approach' in the fired arm
  ok:   no source written
```

changes-1.txt / changes-2.txt (git status of the sandbox after the session; both runs identical
shape): only `.feature-flow/rate-limit/manifest.json` modified and `.feature-flow/rate-limit/architect.md`
added — no source file touched, matching "the architect is read-only":
```
 M .feature-flow/rate-limit/manifest.json
?? .feature-flow/rate-limit/architect.md
```

### control arm (version-flag spec — one sane implementation)

| run | result | assert.sh findings | total_cost_usd | duration_ms | duration_api_ms | num_turns |
|---|---|---|---|---|---|---|
| 1 | PASS | architect.md written; `Recommended:` line; "control arm says one obvious approach"; no `Rejected:` line; no source written | 0.2712201 | 49436 | 49011 | 6 |
| 2 | PASS | architect.md written; `Recommended:` line; "control arm says one obvious approach"; no `Rejected:` line; no source written | 0.3289161 | 68837 | 68400 | 6 |

assert-1.log / assert-2.log (control, verbatim, identical shape both runs):
```
  ok:   architect.md written (/tmp/ff-forward-<tmp>/.feature-flow/version-flag/architect.md)
  ok:   Recommended: line
  ok:   control arm says one obvious approach
  ok:   no Rejected: line
  ok:   no source written
```

changes-1.txt / changes-2.txt: only `.feature-flow/version-flag/manifest.json` modified and
`.feature-flow/version-flag/architect.md` added:
```
 M .feature-flow/version-flag/manifest.json
?? .feature-flow/version-flag/architect.md
```

## 5. Saved architect.md — byte size + word count

**Caveat (mechanical, from the capture script itself, not an interpretation):** the capture
line added to the untracked script is
`find "$tmp" -name architect.md -exec cp {} "$arm_out/architect.md" \;`, executed once per
repeat iteration and writing to the SAME fixed filename `$arm_out/architect.md` each time. With
`--repeat 2` this means run 1's copy is overwritten by run 2's copy — only the LAST run's
architect.md survives on disk per arm. The table below reports the file that exists (which is
run 2's report for both arms); run 1's architect.md was not independently preserved by this
script.

| arm | saved architect.md corresponds to | byte size | word count |
|---|---|---|---|
| fired | run 2 (run 1 overwritten) | 3562 bytes | 494 words |
| control | run 2 (run 1 overwritten) | 3017 bytes | 441 words |

Files: `forward-new/single-architect/fired/architect.md`, `forward-new/single-architect/control/architect.md`
(both under this evidence dir).

## 6. Verdict

Both arms PASS on both runs (2/2 fired PASS, 2/2 control PASS), matching the expected observable:
fired arm produces `Recommended:` + >=1 scored `Rejected:` line and no 'one obvious approach —'
line; control arm produces `Recommended:` + the single 'one obvious approach — <why>' line and no
`Rejected:` line. No source file was written by the architect in any run. Total spend for this
step: **$1.3115**.

## Artifact paths (all under this evidence dir, `forward-new/`)

- `forward-new/summary.md`
- `forward-new/single-architect/fired/run-1.json`, `run-2.json`, `run-1.err`, `run-2.err`
- `forward-new/single-architect/fired/assert-1.log`, `assert-2.log`
- `forward-new/single-architect/fired/changes-1.txt`, `changes-2.txt`
- `forward-new/single-architect/fired/architect.md` (run 2's report — see caveat above)
- `forward-new/single-architect/control/run-1.json`, `run-2.json`, `run-1.err`, `run-2.err`
- `forward-new/single-architect/control/assert-1.log`, `assert-2.log`
- `forward-new/single-architect/control/changes-1.txt`, `changes-2.txt`
- `forward-new/single-architect/control/architect.md` (run 2's report — see caveat above)
