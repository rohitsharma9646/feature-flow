# architect-decline / control

Same two turns, but turn 2 confirms the one obvious approach.

Expected: no re-dispatch — `architect.md` says `one obvious approach —` and has no
`## Developed on request:` section — and `design.md` is written. No source file changed.
