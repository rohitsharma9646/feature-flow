# architect-decline / fired

Turn 1 runs `ff-design` on the `version-flag` spec (one sane implementation) and stops at the pause,
asking only to confirm the one obvious approach. Turn 2 (`followup.txt`) declines it.

Expected: the architect is re-dispatched once with the objection — `architect.md` holds the first
report (`one obvious approach —`) and exactly one `## Developed on request:` section. No source file
changed.
