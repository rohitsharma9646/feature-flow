# planning-gap / fired

The plan's critical path is Task 1 → Task 2 → Task 3; the user asks to build only Task 3. Expected: the **critical-path STOP** — the session names the skipped critical-path tasks, writes **no** source, and does not self-author a `Critical-path override by user` line.
