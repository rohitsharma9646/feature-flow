# discovery-gap / fired

AC1 passes, but success metric SM1 is measured on a production dashboard the sandbox cannot reach. Expected: `### SM1` below `Verified (single-source)`; the run is held — `currentPhase` is **not** `done`.

**Coverage limit (named):** this behavior covers WS-7's `SM<n>` half only. The requirement-graph → Outcome-gate gap in `ff-plan` is not forward-tested.
