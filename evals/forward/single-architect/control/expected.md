# single-architect / control

Same shape as the fired arm, except the `version-flag` spec has one sane implementation (read
`VERSION`, print it behind a `--version` branch) — nothing to fork on.

Expected: `architect.md` is written with a `Recommended:` line and the single line
`one obvious approach — <why>`; no `Rejected:` line; no source file changed.
