# architect-pick / control

Same two turns, but turn 2 accepts the recommendation.

Expected: no re-dispatch — `architect.md` has no `## Developed on request:` section — and
`design.md`'s `## Chosen approach` names the architect's recommended approach. No source file changed.
