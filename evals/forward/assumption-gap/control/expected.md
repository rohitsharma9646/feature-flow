# assumption-gap / control

Same spec, but Assumption 1 is validated (`n`, evidence recorded). Expected: sign-off is recorded —
`signOff.signed == true`, the spec reads `User signed off: yes (...)`, clarify complete.
